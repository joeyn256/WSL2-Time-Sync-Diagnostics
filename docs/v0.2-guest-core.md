# v0.2 guest core — historical documentation path

This path is retained so links created during pre-1.0 development do not break.

The guest-core work introduced during the v0.2 development cycle is carried in
the v0.4.0 public preview. The maintained reference is now:

**[CLI and schema reference](cli-schema.md)**

The package release number is intentionally separate from the report/method
versions. The v0.4.0 preview continues to use `schema_version: 2`,
`raw_bracket_v1`, and `fixed_window_interval_v2`; those identifiers describe
data or analysis contracts, not the package's semantic-version major number.

Historical scope: [issue #2](https://github.com/joeyn256/WSL2-Time-Sync-Diagnostics/issues/2).
